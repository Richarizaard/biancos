mutation createRecipe($input: CreateRecipeInput!) {
  createRecipe(input: $input) {
    recipe {
      name
      description
      toppings {
        id
        name
        description
      }
    }
  }
}
